<Page x:Class="Comfort24.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Comfort24.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginPage" Background="White" >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Авторизация" Grid.Column="1" Grid.Row="0" 
                   FontSize="24" 
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center" 
                   FontFamily="Candara" 
                   FontWeight="Black" 
                   Margin="0,50,0,30"/>
        <StackPanel Grid.Row="1" Grid.Column="1"
                    Margin="0,0,0,20">
            <TextBlock Text="Логин" FontSize="15" HorizontalAlignment="Center"/>
            <TextBox x:Name="login" Height="50"
                     FontSize="16"
                     Padding="10"
                     Margin="0 0 0 20"/>

            <TextBlock Text="Пароль" FontSize="15" HorizontalAlignment="Center"/>
            <PasswordBox x:Name="password" Height="50"
         FontSize="16"
         Padding="10"
         Margin="0 0 0 20"/>

        </StackPanel>
        <StackPanel Grid.Row="2" Grid.Column="1" >
            <Button Name="enter" Background="#355CBD"
                    Foreground="White"  Content="Вход" 
                    FontSize="18" Height="50" Margin="0 0 0 10" 
                    Click="enter_Click" />

            <Button Name="Registration" BorderBrush="#355CBD"
           Content="Ругистрация" FontWeight="Bold"
                    Foreground="#355CBD"
         FontSize="18" Height="50" Margin="0 0 0 10"
                    Click="Registration_Click" 
          />

        </StackPanel>
    </Grid>
</Page>
